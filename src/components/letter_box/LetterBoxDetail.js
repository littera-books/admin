import React from 'react';

// Styled
import Wrapper from '../../styled_base/Wrapper';

const LetterBoxDetail = () => (
  <Wrapper.FlexWrapper>
    <p>detail</p>
  </Wrapper.FlexWrapper>
);

export default LetterBoxDetail;
