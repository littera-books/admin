import React from 'react';

// Styled
import Wrapper from '../../../styled_base/Wrapper';

const ProductList = () => (
  <Wrapper.ListWrapper>
    <p>list</p>
  </Wrapper.ListWrapper>
);

export default ProductList;
