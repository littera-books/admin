import React from 'react';

// Styled
import Wrapper from '../../../styled_base/Wrapper';

const Product = () => (
  <Wrapper.FlexWrapper>
    <h1>product</h1>
  </Wrapper.FlexWrapper>
);

export default Product;
